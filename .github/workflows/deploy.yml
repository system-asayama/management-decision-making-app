name: Deploy

on:
  push:
    branches: [ claude/analyze-repository-content-9ljcA ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-production:
    if: github.ref == 'refs/heads/claude/analyze-repository-content-9ljcA'
    runs-on: ubuntu-latest
    steps:
      - name: Deploy via SSH (production)
        uses: appleboy/ssh-action@v1.0.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ secrets.SSH_HOST_PRODUCTION }}
          username: ${{ secrets.SSH_USER_PRODUCTION }}
          port: ${{ secrets.SSH_PORT_PRODUCTION }}
          key: ${{ secrets.SSH_PRIVATE_KEY_PRODUCTION }}
          envs: GITHUB_TOKEN
          script: |
            echo "[DEBUG] target_dir=~/apps/management-decision-making-app" && echo "[DEBUG] full_name=system-asayama/management-decision-making-app" && echo "[DEBUG] branch=claude/analyze-repository-content-9ljcA" && echo "[DEBUG] deploy_mode=docker-compose" && echo "[DEBUG] hostname=$(hostname)" && echo "[DEBUG] whoami=$(whoami)" && echo "[DEBUG] git --version: $(git --version 2>&1)" && echo "[DEBUG] docker --version: $(docker --version 2>&1)" && echo "[DEBUG] docker compose version: $(docker compose version 2>&1)" && echo "[DEBUG] dir exists? $(test -d ~/apps/management-decision-making-app && echo YES || echo NO)" && echo "[DEBUG] .git exists? $(test -d ~/apps/management-decision-making-app/.git && echo YES || echo NO)" && if [ -d ~/apps/management-decision-making-app/.git ]; then echo "[DEBUG] taking git pull path" && cd ~/apps/management-decision-making-app && git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/system-asayama/management-decision-making-app.git && echo "[DEBUG] git remote URL updated" && (git pull --rebase --autostash 2>/dev/null || git pull) && echo "[DEBUG] git pull OK"; else echo "[DEBUG] taking git clone path" && mkdir -p "$(dirname ~/apps/management-decision-making-app)" && rm -rf ~/apps/management-decision-making-app && git clone --branch claude/analyze-repository-content-9ljcA https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/system-asayama/management-decision-making-app.git ~/apps/management-decision-making-app && echo "[DEBUG] git clone OK"; fi && echo "[DEBUG] post-pull, ls ~/apps/management-decision-making-app:" && ls -la ~/apps/management-decision-making-app | head -20 && cd ~/apps/management-decision-making-app && echo "[DEBUG] in ~/apps/management-decision-making-app, WEB_PORT=8082" && echo "[DEBUG] compose files:" && ls -la docker-compose* compose* 2>/dev/null && echo "[DEBUG] running docker compose up..." && WEB_PORT=8082 docker compose pull 2>/dev/null || true; WEB_PORT=8082 docker compose up -d --build --remove-orphans && echo "[DEBUG] docker ps:" && docker ps --filter "label=com.docker.compose.project" --format "{{.Names}}\t{{.Status}}\t{{.Ports}}"

# Auto-generated by github-support-app for Docker 化
# `.env` を VPS の ~/apps/management-decision-making-app/.env に置くと docker compose が読みます。
# `.gitignore` に `.env` を入れてリポジトリにはコミットしないでください。

# Web の外部ポート (github-support-app が自動採番するので空でも OK)
# WEB_PORT=8080

# Flask SECRET_KEY (本番では必ず変更)
SECRET_KEY=change-me-in-production
